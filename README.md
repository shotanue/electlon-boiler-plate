# electron app boiler plate, with parcel. 

### start electron app 
```bash
// init dependencies
yarn install

// make parcel watch files
yarn watch

// start electron (open another shell and run this command)
yarn start
``` 

### build mac app
```bash
yarn build
```